<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">

        <item
                android:id="@+id/nav_home"
                android:title="Inici"
                android:icon="@drawable/ic_home"
        />

        <item
                android:id="@+id/nav_login"
                android:title="Iniciar Sessió"
                android:icon="@drawable/ic_login"
        />

        <item
                android:id="@+id/nav_register"
                android:title="Registrar"
                android:icon="@drawable/ic_register_menu"
        />

        <item
                android:id="@+id/nav_ranking"
                android:title="Ranking"
                android:icon="@drawable/ic_trophy"
        />

        <item
                android:id="@+id/nav_options"
                android:title="Opcions"
                android:icon="@drawable/ic_settings"
        />

        <item
                android:id="@+id/nav_signoff"
                android:title="Tancar Sessió"
                android:icon="@drawable/ic_signoff"

        />


    </group>
</menu>